# Environment configuration for GreenOps Agent
# Update values as needed before running the application

# GCP & BigQuery
PROJECT_ID=autogreenops
REGION=us-central1
CLUSTER_NAME=boutique-cluster
NAMESPACE=boutique
BILLING_TABLE=autogreenops.billing_dataset.gcp_billing_export_resource_v1_018317_83DA9C_15D7B1
SERVICE_ACCOUNT=gcp_billing_export_resource_v1_018317_83DA9C_15D7B1

# Paths
GOOGLE_APPLICATION_CREDENTIALS=secrets/gcp-service-account.json

# Deployment defaults (used by scripts/deploy_cloud_run.sh)
# Override these as needed. SERVICE_NAME is the Cloud Run service name.
SERVICE_NAME=greenops-agent
# Artifact Registry repository name and location (region)
ARTIFACT_REPO_NAME=greenops-repo
ARTIFACT_LOCATION=us-central1
# Optional: explicitly set the service account email to use for Cloud Run (Workload Identity)
# If left blank the script will attempt to detect from the key or create `greenops-run-sa@${PROJECT_ID}.iam.gserviceaccount.com`
SA_EMAIL=

# Carbon Footprint API
CARBON_API_ENDPOINT=https://carbonfootprint.googleapis.com/v1
# If using a specific service or version, set it here
CARBON_API_KEY=

# Vertex AI / Gemini
VERTEX_PROJECT=autogreenops
VERTEX_REGION=us-central1
# Model id: update to the Gemini model you want to use, or set to 'gemini' as a placeholder
VERTEX_MODEL_ID=alloy-medium
VERTEX_API_ENDPOINT=https://us-central1-aiplatform.googleapis.com

# Streamlit
STREAMLIT_PORT=8080

# App config
REFRESH_INTERVAL_SECONDS=300

# Optional tuning
BILLING_QUERY_LIMIT=10000

# Logging
LOG_LEVEL=INFO
